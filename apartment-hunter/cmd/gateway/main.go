package main

func main() {
	// env := env.New(env.NewConfig(env.OptionPanicOnMissing).
	// 	AddFlagEnv("DEBUG", "debug", "false", "Enable debug logging").
	// 	AddEnv("FIREBASE_CREDENTIALS", "", "Firebase credentials path"))

	// opt := option.WithCredentialsFile(env.Get("FIREBASE_CREDENTIALS"))
	// app, err := firebase.NewApp(context.Background(), nil, opt)
	// if err != nil {
	// 	log.Fatalf("error initializing app: %v\n", err)
	// }
}
